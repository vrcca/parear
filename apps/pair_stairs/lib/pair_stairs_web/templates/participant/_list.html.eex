<table>
    <thead>
        <tr>
            <th><%= gettext("Name") %></th>
            <th class="actions"><%= gettext("Actions") %></th>
        </tr>
    </thead>
    <tbody>
        <%= for participant <- sorted_list(@participants) do %>
            <tr>
                <td><%= participant.name %></td>
                <td><%= remove_participant_button(@conn, participant.id) %></td>
            </tr>
        <% end %>
    </tbody>
</table>
